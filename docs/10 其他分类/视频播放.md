# 📺 多视频播放器（批量管理）

点击按钮展开/关闭视频，支持批量添加BV号。

---

## 🎬 视频列表

### 视频 1
[▶ 播放](#play-1) [✖ 关闭](#close-1) [🗑 删除](#remove-1)

<div id="player-1" style="display: none; margin: 10px 0 20px;">

<iframe 
  src="https://player.bilibili.com/player.html?bvid=BV1xx411c79H&page=1&high_quality=1&autoplay=0" 
  width="100%" 
  height="400" 
  frameborder="0"
  allowfullscreen>
</iframe>

</div>

---

### 视频 2
[▶ 播放](#play-2) [✖ 关闭](#close-2) [🗑 删除](#remove-2)

<div id="player-2" style="display: none; margin: 10px 0 20px;">

<iframe 
  src="https://player.bilibili.com/player.html?bvid=BV1GJ411x7h7&page=1&high_quality=1&autoplay=0" 
  width="100%" 
  height="400" 
  frameborder="0"
  allowfullscreen>
</iframe>

</div>

---

### 视频 3
[▶ 播放](#play-3) [✖ 关闭](#close-3) [🗑 删除](#remove-3)

<div id="player-3" style="display: none; margin: 10px 0 20px;">

<iframe 
  src="https://player.bilibili.com/player.html?bvid=BV1ZJ41197hQ&page=1&high_quality=1&autoplay=0" 
  width="100%" 
  height="400" 
  frameborder="0"
  allowfullscreen>
</iframe>

</div>

---

## 📝 批量添加视频

每行输入一个BV号或完整链接：





[➕ 批量添加](#batch-add) [🗑️ 清空所有](#clear-all)

---

<script>
// 视频播放器控制
(function() {
  // 获取所有播放器容器
  const players = {
    1: document.getElementById('player-1'),
    2: document.getElementById('player-2'),
    3: document.getElementById('player-3')
  };
  
  // 获取所有iframe
  const iframes = {
    1: players[1]?.querySelector('iframe'),
    2: players[2]?.querySelector('iframe'),
    3: players[3]?.querySelector('iframe')
  };
  
  // 保存原始src
  const originalSrcs = {
    1: iframes[1]?.src,
    2: iframes[2]?.src,
    3: iframes[3]?.src
  };

  // 处理所有链接点击
  document.querySelectorAll('a').forEach(link => {
    link.addEventListener('click', (e) => {
      e.preventDefault();
      const href = link.getAttribute('href');
      
      // 播放按钮
      if (href.includes('play-')) {
        const num = href.split('-')[1];
        if (players[num]) {
          // 先关闭所有其他播放器
          for (let key in players) {
            if (key !== num && players[key]) {
              players[key].style.display = 'none';
              if (iframes[key]) {
                iframes[key].src = '';
              }
            }
          }
          // 打开当前播放器
          players[num].style.display = 'block';
          if (iframes[num] && !iframes[num].src) {
            iframes[num].src = originalSrcs[num];
          }
        }
      }
      
      // 关闭按钮
      if (href.includes('close-')) {
        const num = href.split('-')[1];
        if (players[num]) {
          players[num].style.display = 'none';
          if (iframes[num]) {
            iframes[num].src = ''; // 停止播放
          }
        }
      }
      
      // 删除按钮
      if (href.includes('remove-')) {
        const num = href.split('-')[1];
        if (confirm('确定删除这个视频吗？')) {
          const item = players[num]?.closest('div')?.parentNode;
          if (item) {
            item.style.display = 'none';
          }
        }
      }
      
      // 批量添加
      if (href === '#batch-add') {
        const bvList = prompt('请输入BV号（每行一个）：', 'BV1xx411c79H\nBV1GJ411x7h7\nBV1ZJ41197hQ');
        if (bvList) {
          alert('已添加 ' + bvList.split('\n').length + ' 个视频（演示模式，实际添加需要刷新页面）');
        }
      }
      
      // 清空所有
      if (href === '#clear-all') {
        if (confirm('确定清空所有视频吗？')) {
          for (let key in players) {
            if (players[key]) {
              players[key].style.display = 'none';
              if (iframes[key]) {
                iframes[key].src = '';
              }
            }
          }
        }
      }
    });
  });
})();
</script>

<style>
/* 基础样式 */
body {
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
  max-width: 900px;
  margin: 0 auto;
  padding: 20px;
  background: #f8fafc;
}

h1 {
  color: #00aeec;
  border-bottom: 3px solid #00aeec;
  padding-bottom: 10px;
}

a {
  display: inline-block;
  padding: 8px 20px;
  margin: 5px;
  text-decoration: none;
  border-radius: 30px;
  font-weight: 500;
  transition: all 0.2s;
}

/* 播放按钮 */
a[href*="play"] {
  background: #00aeec;
  color: white;
  border: 1px solid #0096d0;
}

a[href*="play"]:hover {
  background: #33c0f0;
  transform: translateY(-2px);
  box-shadow: 0 5px 15px rgba(0,174,236,0.3);
}

/* 关闭按钮 */
a[href*="close"] {
  background: #f1f3f5;
  color: #4a5568;
  border: 1px solid #dee2e6;
}

a[href*="close"]:hover {
  background: #e9ecef;
}

/* 删除按钮 */
a[href*="remove"] {
  background: #fff5f5;
  color: #e53e3e;
  border: 1px solid #feb2b2;
}

a[href*="remove"]:hover {
  background: #fed7d7;
}

/* 批量操作按钮 */
a[href="#batch-add"], a[href="#clear-all"] {
  background: #48bb78;
  color: white;
  border: none;
  padding: 12px 25px;
  font-size: 1.1rem;
}

a[href="#clear-all"] {
  background: #fc8181;
}

a[href="#batch-add"]:hover, a[href="#clear-all"]:hover {
  transform: translateY(-2px);
  box-shadow: 0 5px 15px rgba(72,187,120,0.3);
}

/* 视频区域 */
iframe {
  border-radius: 12px;
  box-shadow: 0 10px 30px rgba(0,0,0,0.15);
  margin: 15px 0;
  background: #1a1e24;
}

/* 批量输入框（模拟） */
pre {
  background: #2d3748;
  color: #cbd5e0;
  padding: 15px;
  border-radius: 10px;
  font-family: 'SF Mono', monospace;
  margin: 15px 0;
}

hr {
  border: none;
  border-top: 2px dashed #cbd5e0;
  margin: 25px 0;
}
</style>

---

## 📖 使用说明

1. **播放视频**：点击每个视频的 `▶ 播放` 按钮展开播放器
2. **关闭视频**：点击 `✖ 关闭` 按钮收起并停止播放
3. **删除视频**：点击 `🗑 删除` 移除该视频条目
4. **批量添加**：在下方输入框中按行输入BV号，点击批量添加
5. **清空所有**：一键关闭并隐藏所有视频

> 💡 提示：关闭视频时会自动清空iframe源，彻底停止播放和加载
